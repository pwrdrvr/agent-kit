# Changesets

This directory is managed by [changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to record a version bump for one or more packages; CI versions
and publishes on merge to `main`. See [README.md](../README.md) for the release flow.
