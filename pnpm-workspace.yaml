packages:
  - "packages/*"
  - "examples/*"

# Allow the freshly-published Codex protocol types regardless of the global
# minimumReleaseAge gate — they are the pinned contract for the Codex adapter.
minimumReleaseAgeExclude:
  - "@pwrdrvr/codex-app-server-protocol"
